{
  "scripts": {
    "test1": "echo test1 && exit 0",
    "test2": "echo test2 && exit 1",
    "test3": "echo test2 && exit 2",
    "preother": "echo preother && exit 3",
    "other": "echo other && exit 4",
    "postother": "echo postother && exit 5",
    "node-bin": "node-bin"
  }
}
